const commInterface = require("../../classes/commInterface/CommInterface");

describe("CommInterface", () => {
  it("should already create commInterface object", () => {
    expect(commInterface).toBeDefined();
  });
});
